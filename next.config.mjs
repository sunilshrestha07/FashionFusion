/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com','i.pinimg.com','lh3.googleusercontent.com'],
      },
};

export default nextConfig;

